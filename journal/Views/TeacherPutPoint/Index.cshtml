@model journal.ViewModels.TeacherPutPointViewModel
@{
    ViewBag.Title = "Index";
}
<script src="~/Scripts/controllers/teacherPutPoint.js"></script>
<h2>Index</h2>

<div id="data" class="row">
    <div class="col-md-12">

        <div class="form-inline">
            <label for="subject">Subject</label>
            <select class="form-control" id="subject">
                <option></option>
                @foreach (var item in Model.Subjects)
                {
                    <option value="@item.Value">@item.Text</option>
                }
            </select>           
            <button type="button" class="btn btn-default" onclick="Search()">Search</button>
        </div>
        <br />





        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>
                            Ім'я Прізвище
                        </th>
                        <th class="select">                            
                            <select id="selectPointLevels" >
                                <option></option>
                                @foreach (var item in Model.PointLevels)
                                {
                                    <option value="@item.Value">@item.Text</option>
                                }
                            </select>

                        </th>
                    </tr>
                </thead>
                <tbody id="tablebody">   
                    @*@foreach (var user in Model.Users)
                    {
                        <tr class="userRow" id="@user.Value">
                            <td>
                                @user.Text                                
                            </td>                        
                            <td>
                                <select class="selectedPointValues">
                                    <option></option>
                                    @foreach (var item in Model.PointValues)
                                    {
                                        <option value="@item.Value">@item.Text</option>
                                    }
                                </select>
                            </td>
                        </tr>
                    }*@                    
                </tbody>
                <tr>
                    <td></td>

                    <td>
                        <div class="form-group">
                            <div class="col-md-offset-2 col-sm-offset-3 col-md-10">
                                <a id="">Edit</a>
                                <button class="btn btn-default" type="button" onclick="addPointToPupil()">Add</button>
                            </div>
                        </div>
                    </td>
                </tr>              
            </table>
        </div>
     </div>
</div>
